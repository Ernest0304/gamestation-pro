# Ops Audit — 2026-07-03 — Tier 0 batch (a29d68a), floor-workflow lens

## 🚨 Operational risks

1. **Tonight's close can be BLOCKED (transition day).** Old code stamped close_date with the calendar date at close time — a ~00:30 close last night wrote `close_date='2026-07-03'`. New code closes business date `2026-07-03` tonight (~01:00). `history.js:641` finds that stale row → modal shows "已结账记录", confirm button hidden (`history.js:725`); insert would also hit UNIQUE (branch_id, close_date) (`009:110`). Cashier cannot close tonight; no in-app fix. **Check daily_closes for a 2026-07-03 row NOW; rename/delete via SQL before 01:00.** Also: all after-midnight historical closes hold old-window snapshots — recomputed business-day reports won't match them (00:00–01:00 sales now shift to the prior business day). Migration 012 didn't touch daily_closes.

2. **Reprint of a VOIDED order prints a clean receipt** (`js/orders.js:547` — button unconditional; `js/pos.js:1520` — no VOID/REPRINT marker). Fraud path: take cash → receipt → void (drawer balances) → customer "proves" payment with a pristine reprint. Print **已作废 VOID** on voided orders and a REPRINT banner + count on all reprints.

3. **Held orders live on ONE iPad only** (`js/pos.js:106` localStorage). Two-iPad shop: iPad B never sees iPad A's 挂单 — the 取单 button doesn't even render there. Overnight-forgotten hold shows time only, no date (`pos.js:172`); prices frozen at hold time; Safari storage eviction = silent loss; close-day gives no "N orders still held" warning. SOP until synced: hold/recall on the same iPad only; check 取单 before daily close.

## ⚠️ Process gaps

- **Offline mid-split:** confirm greys out (`css/style.css:160-164`), red banner, cart survives — good; split is atomic so no half-recorded tenders. But `navigator.onLine` misses router-up/internet-down. **Paper fallback SOP:** duplicate receipt pad, note items/tender/time, ring in after recovery.
- **Manager PIN:** Settings itself is ungated — any staff can overwrite the PIN without knowing the old one (`js/settings.js:239`). That's the recovery when the PIN-knower is absent, but it also defeats the control. No way to unset once set (blank = keep). Rotate on staff exit; gate Settings next.
- **Opening float:** `branches.opening_float` is read (`store.js:112,1867`) but **no UI sets it — both branches are $0** (June backup). Feature inert until SQL. Cashier sees float only at close, never at open — add a Settings field + open-day count.

## 📋 QA tonight

1. 00:45 close → confirm button present, close succeeds.
2. Void today's cash order → Z-Report cash drops.
3. Reprint a voided order → inspect what it prints.
